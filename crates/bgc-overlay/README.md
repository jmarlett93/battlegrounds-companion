# bgc-overlay

GTK4 + gtk4-layer-shell overlay window: status, races, tier buttons 1–6, card name list, last opponent board.

Runs on the GTK main thread. The app pushes `OverlaySnapshot` updates over a channel.
