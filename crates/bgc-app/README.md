# bgc-app

Binary crate (`bgc`). Boots storage + catalog, discovers Power.log, runs the GTK overlay, and tails logs on a background thread.

`--demo` injects fake races/opponent board and writes one combat telemetry row without Hearthstone.
