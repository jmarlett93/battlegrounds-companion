# bgc-state

Pure functional Battlegrounds phase/state: Idle → Lobby → Match → Combat, available races, and opponent board history.

No I/O. Call `apply` with events from the log tailer or `--demo` injectors.
